name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Pull latest code
        shell: cmd
        run: |
          git config --global --add safe.directory D:/mobile_netra
          if not exist D:\mobile_netra\.git (
            git clone http://pmk:12345678@211.56.248.9:3000/miraesoftware/mobile_netra-test-.git D:\mobile_netra
          ) else (
            git -C D:\mobile_netra fetch http://pmk:12345678@211.56.248.9:3000/miraesoftware/mobile_netra-test-.git && git -C D:\mobile_netra reset --hard origin/main
          )

      - name: Stop PM2
        shell: cmd
        run: pm2 stop mobile-netra 2>nul & exit 0

      - name: Install dependencies
        working-directory: D:\mobile_netra
        shell: cmd
        run: npm ci

      - name: Build
        working-directory: D:\mobile_netra
        shell: cmd
        run: npm run build

      - name: PM2 restart
        working-directory: D:\mobile_netra
        shell: cmd
        run: |
          pm2 restart mobile-netra || pm2 start ecosystem.config.js
          pm2 save
